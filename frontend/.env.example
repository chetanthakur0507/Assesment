# Only needed if the backend isn't reachable via the Vite dev proxy
# (e.g. deploying frontend and backend separately). If unset, the app
# talks to the backend on the same origin / via the dev proxy.
VITE_WS_URL=ws://localhost:8787/call
